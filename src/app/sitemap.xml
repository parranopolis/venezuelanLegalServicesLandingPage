// app/sitemap.ts
import type { MetadataRoute } from "next";

const SITE = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";

/**
 * Add any additional static routes to the array if/when you create them,
 * e.g. "/servicios", "/faq", "/contacto".
 */
export default function sitemap(): MetadataRoute.Sitemap {
  const now = new Date();

  return [
    {
      url: `${SITE}/`,
      lastModified: now,
      changeFrequency: "weekly",
      priority: 1,
    },
    // Example extra routes (uncomment when pages exist):
    // { url: `${SITE}/servicios`, lastModified: now, changeFrequency: "monthly", priority: 0.8 },
    // { url: `${SITE}/faq`,       lastModified: now, changeFrequency: "monthly", priority: 0.7 },
    // { url: `${SITE}/contacto`,  lastModified: now, changeFrequency: "yearly",  priority: 0.6 },
  ];
}
